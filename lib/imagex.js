const path = require('path')
const chalk = require('chalk')

module.exports = (opt = {}) => {
  const {
    src,  
    dest,
    prefix,
    isCrop,
    isMini,
    isUnique,
    isSprites,
    isInfo,
    isReplace,
    spritesCount,
    mode,
  } = opt
  
  return new Promise((resolve, reject) => {

  })
}